🌊 NemoViewer: Визуализатор данных океанографической модели NEMO
Проект предназначен для представления коллегам-океанологам результатов работы модели NEMO. Позволяет просматривать карты гидрологических параметров, строить TS-диаграммы (зависимость температуры от солености), вертикальные разрезы и анимированные траектории частиц по полю течений.

🚀 Основные возможности
Интерактивные карты: Выбор даты, региона, глубины и параметра (температура, соленость, течения, лед, уровень моря).

TS-профили и графики: Построение профилей по глубине для выбранной точки с наложением климатологических данных.

Вертикальные разрезы: Интерактивное построение разрезов между двумя точками с выбором глубины построения.

Анимация частиц: Визуализация траекторий движущихся частиц по полю скоростей (в разработке).

Проекции: Поддержка картографических проекций для Мирового океана, Арктики и Антарктики.

🛠 Технологический стек
Компонент	Технология
Бэкенд (API)	Genie.jl (веб-фреймворк на Julia)
База данных	PostgreSQL с расширением PostGIS
Фронтенд	Чистый HTML, CSS, JavaScript (Canvas/WebGL)
Графики	Plots.jl (GR бэкенд), Chart.js
Картография	Кастомные преобразования координат (proj4.js)
Деплой	Сервер на 0.0.0.0:8000 (настроено в app.jl)

📁 Структура проекта
text
NemoViewer/
├── app.jl                          # Точка входа, инициализация сервера
├── run.jl                          # Скрипт запуска приложения
├── src/
│   ├── config/
│   │   └── database.jl            # Настройки подключения к БД
│   ├── utils/
│   │   ├── database_functions.jl  # Основные функции работы с БД
│   │   └── particle_engine.jl     # Ядро для расчета траекторий частиц (новый)
│   └── routes/
│       ├── main_routes.jl         # Главный маршрут (рендеринг HTML)
│       ├── api_routes.jl          # Все API-эндпоинты (данные, графики, частицы)
│       ├── javascript_code.jl     # Весь клиентский JavaScript (генерируемый)
│       ├── html_templates.jl      # HTML-шаблоны страницы
│       └── css_styles.jl          # Стили (CSS в виде строк Julia)
├── public/static/
│   ├── maps/                      # Предварительно сгенерированные статические карты
│   │   └── [дата]/
│   │       ├── wo_Tz0p5_000.png
│   │       └── ... (png, gif)
│   ├── js/                        # (Планируется) Отдельные JS-файлы (particles.js)
│   └── color_schemes.jl           # Цветовые палитры для графиков
└── (README.md)                    # Этот файл

🔧 Установка и запуск
Предварительные требования:

Установленный Julia.

Запущенный сервер PostgreSQL с созданной БД, пользователем и расширением PostGIS.

Настроенные таблицы данных в соответствии со схемой (см. ниже).

Настройка конфигурации:

Отредактируйте файл src/config/database.jl, указав параметры вашей БД (dbname, user, password, host, port).

Запуск приложения:

В корневой директории проекта выполните в терминале:

bash
julia run.jl
Сервер запустится на http://0.0.0.0:8000 (по умолчанию).

🗄️ Структура базы данных
Основная таблица с оперативными данными модели организована в секции (партиции) по дате.

sql
CREATE TABLE IF NOT EXISTS public._nemo
(
    dat date,
    lon numeric(6,2),
    lat numeric(6,2),
    par jsonb,                    -- Основные данные: массивы параметров по горизонтам
    geom geometry(Point,4326),    -- Пространственный индекс
    CONSTRAINT _nemo_dat_lon_lat_key UNIQUE (dat, lon, lat)
) PARTITION BY RANGE (dat);

-- Пример секции для конкретной даты:
-- CREATE TABLE "2025-12-11"."_nemo_2025-12-11" PARTITION OF public._nemo ...
Поле par (JSONB): Содержит массив объектов, где каждый объект — данные для одного горизонтра глубины. Каждый объект содержит поля:

depth: Глубина горизонта (скаляр, напр., 0.51).

u, v: Массивы компонент скорости течения по времени (анализ + прогноз).

tw, s: Массивы температуры и солености.

iceconc, icethic, ssh, mld и др. — другие параметры.

🔄 Ключевые API-эндпоинты
Метод	Путь	Назначение
GET	/	Главная страница с интерфейсом.
POST	/api/point_data	Получение всех данных в ближайшей к клику точке.
POST	/api/plot_depth	Построение графика параметра по глубине для последней выбранной точки.
POST	/api/plot_ts	Построение TS-диаграммы.
POST	/api/section_plot	Построение вертикального разреза между двумя точками.
POST	/api/particles/velocity-field	(Новый) Возвращает поле скоростей для сетки.
POST	/api/particles/generate-seeds	(Новый) Генерирует начальные позиции для частиц.

🎯 Разработка и текущий фокус
Основное направление текущей разработки — модуль анимации движущихся частиц.

Цель: Создать анимацию, аналогичную windy.com, с наложением на статические карты параметров.

Архитектура:

particle_engine.jl: Вычисление траекторий на сервере с использованием оптимизированных PostGIS-запросов (использование ST_Within, пространственных индексов, TABLESAMPLE).

Новые API (/api/particles/*): Предоставление данных о скоростях и начальных позициях.

Клиентская часть (Canvas/WebGL): Рендеринг тысяч частиц с эффектами шлейфов в реальном времени.

Актуальные задачи:

Исправление и оптимизация SQL-запросов в particle_engine.jl.

Реализация билинейной интерполяции скорости для произвольных точек.

Интеграция управления анимацией (плотность частиц, скорость) в интерфейс.



